var defaults = require('./defaults');
var games = require('./games');
var genres = require('./genres');
var platforms = require('./platforms');

module.exports = [].concat(defaults, games, genres, platforms);